<!DOCTYPE html>
<html class="h-100">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Dodd Group Client Portal | <% if(client){%> <%= client.businessName %> <% } %></title>
    <meta name="author" content="Ashton Redman">
    <meta name="description" content="">
    <meta name="keywords" content="">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css">
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="//fonts.googleapis.com/css?family=font1|font2" type="text/css">
    <link href="/css/summernote-bs4.css" rel="stylesheet">
    <link rel="stylesheet" href="https://ajax.googleapis.com/ajax/libs/jqueryui/1.12.1/themes/smoothness/jquery-ui.css">
    <link rel="stylesheet" href="//cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css" type="text/css">
    <link rel="stylesheet" href="/css/jquery.steps.css" type="text/css">
    <link rel="stylesheet" href="/css/jquery.schedule.css" type="text/css">
    <link rel="stylesheet" href="/css/stylesheet.css" type="text/css">
</head>

<body class="h-100 dodd-contract">
    <div class="h-100">
        <!-- Navigation -->
        <nav class="navbar navbar-expand-md fixed-top navbar-light">
            <a class="navbar-brand dodd-logo" href="/contract"><img src="/img/Property_care.png" title="Contract Home"></a>
            <a href="/" id="logo"><img class="dodd-logo d-none d-lg-block" id="logo" src="/assets/images/DG.PNG" title="Home" alt="Dodd Group Logo"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
                <ul class="navbar-nav custom-nav justify-content-end">
                    <% if(user){
                    if(user.isClientAdmin || user.isSuperUser){
                        if(user.isClientAdmin){ %>
                            <li class="nav-item">
                                <a class="nav-link" href="/client/<% user.id %>">
                                    Client Home
                                </a>
                            </li>
                        <% }
                        if(user.isSuperUser){ %>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="accountDropdown" role="button" data-toggle="dropdown"
                                    aria-haspopup="true" aria-expanded="false">
                                    Client Home
                                </a>
                                <div class="dropdown-menu" aria-labelledby="accountrDropdown">
                                    <a class="dropdown-item" href="#">Select Client</a>
                                </div>
                            </li>
                        <% }
                    }
                    if(user.isSuperUser){ %>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">
                            Site Admin
                        </a>
                    </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link" href="/contract">Contract</a>
                    </li>
                    <% } %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="accountDropdown" role="button" data-toggle="dropdown"
                            aria-haspopup="true" aria-expanded="false">
                            My Account
                        </a>
                        <div class="dropdown-menu" aria-labelledby="accountrDropdown">
                            <a class="dropdown-item" href="/contract/account">Account Detials</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="/logout">Log Out</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/help">
                            Help
                        </a>
                    </li>
                </ul>
            </div>
        </nav>
        <!-- Alerts -->

        <!-- Alert Card -->
        <div id="dodd-alert-cards">
            <% if(fail && fail.length > 0) { %>
            <div class="alert alert-danger alert-dismissible fade show dodd-alert-floating" role="alert">
                <%= fail %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <% } %>
            <% if(success && success.length > 0) { %>
            <div id="#success-alert" class="alert alert-success alert-dismissible fade show dodd-alert-floating"
                role="alert">
                <%= success %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <% } %>
        </div>